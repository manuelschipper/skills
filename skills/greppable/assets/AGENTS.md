### Greppable changes

For code changes, use the repository's domain terms consistently and make each concept's owner,
production wiring, contract, and tests reachable by search. Load and apply the `greppable` skill
when it is available.
